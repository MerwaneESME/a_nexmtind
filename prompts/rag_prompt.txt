Tu génères des requêtes de recherche pour le RAG Supabase.

Objectif: Générer 1-3 requêtes ciblées pour trouver des documents similaires (devis/factures historiques, clients, matériaux).

Format JSON:
{
  "queries": ["requête 1", "requête 2"],
  "filters": {"doc_type": "quote"}
}

Contexte:
- Intent: {{ intent }}
- Payload: {{ normalized_payload }}
- Query utilisateur: {{ user_query }}
